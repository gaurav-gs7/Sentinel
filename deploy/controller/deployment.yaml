apiVersion: apps/v1
kind: Deployment
metadata:
  name: attesta-controller
  namespace: attesta-system
  labels:
    app.kubernetes.io/name: attesta-controller
    app.kubernetes.io/managed-by: attesta
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: attesta-controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: attesta-controller
        app.kubernetes.io/managed-by: attesta
    spec:
      serviceAccountName: attesta-controller
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: controller
          image: ghcr.io/example/attesta-controller:latest
          imagePullPolicy: IfNotPresent
          args:
            - --resync=30s
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: ["ALL"]
