apiVersion: v1
kind: Service
metadata:
  name: {{ .Service.Name }}
  namespace: {{ .Service.Namespace }}
spec:
  selector:
    app.kubernetes.io/name: {{ .Service.Name }}
  ports:
    - name: http
      port: 80
      targetPort: http
